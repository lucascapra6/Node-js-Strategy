-- cria uma imagem do postgres
docker run \
--name postgres \
-e POSTGRES_USER=lucascapra6 \
-e POSTGRES_PASSWORD=Doboldo123 \
-e POSTGRES_DB=heroes \
-d \
postgres

-- checa os containers
docker ps

-- entra no container
docker exec -it postgres /bin/bash